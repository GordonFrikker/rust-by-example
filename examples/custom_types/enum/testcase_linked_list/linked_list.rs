use List::*;

enum List {
    // Cons: Кортежная структура, которая оборачивает элемент 
    // и устанавливает указатель на следующий узел
    Cons(u32, Box<List>),
    // Nil: Узел обозначающий конец связанного списка
    Nil,
}

// Методы могут быть присоединены к перечислителю
impl List {
    // Создаем пустой список
    fn new() -> List {
        // `Nil` имеет тип `List`
        Nil
    }

    // Функция, которая принимает список и возвращает тот же список, 
    // но с новым элементом в начале
    fn prepend(self, elem: u32) -> List {
        // `Cons` также имеет тип `List`
        Cons(elem, Box::new(self))
    }

    // Возвращаем длину списка
    fn len(&self) -> u32 {
        // `self` должен быть сопоставлен (проверен на соответствие), 
        // поскольку поведение этого метода зависит от варианта `self`
        // `self` имеет тип `&List`, а `*self` имеет тип `List`, сопоставление на
        // конкретном типе `T` предпочтительнее, чем сопоставление по ссылке `&T`
        match *self {
            // Не может взять владение за конец, т.к. `self` заимствован;
            // вместо этого получает ссылку на конец
            Cons(_, ref tail) => 1 + tail.len(),
            // Базовый случай: Пустой список имеет нулевую длину
            Nil => 0
        }
    }

    // Возвращаем представление списка как (размещенной в куче) строки
    fn stringify(&self) -> String {
        match *self {
            Cons(head, ref tail) => {
                // `format!` похож на `print!`, но возвращает строку
                // размещенную в куче, вместо вывода на консоль
                format!("{}, {}", head, tail.stringify())
            },
            Nil => {
                format!("Nil")
            },
        }
    }
}

fn main() {
    // Создаем пустой связанный список
    let mut list = List::new();

    // Присоединяем некоторые элементы
    list = list.prepend(1);
    list = list.prepend(2);
    list = list.prepend(3);

    // Отображаем окончательное состояние списка
    println!("linked list has length: {}", list.len());
    println!("{}", list.stringify());
}
